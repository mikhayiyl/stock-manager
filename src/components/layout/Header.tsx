export function Header() {
  return (
    <header className="h-16 w-full bg-white border-b px-4 flex items-center justify-between">
      <h1 className="text-lg font-semibold">Dashboard</h1>
      {/* Add theme toggle or user info here later */}
    </header>
  );
}
